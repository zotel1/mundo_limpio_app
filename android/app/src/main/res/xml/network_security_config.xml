<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">mundo-limpio-backend.onrender.com</domain>
        <!-- Pin the current certificate's SHA-256 hash -->
        <!-- Update this when the certificate changes -->
    </domain-config>
</network-security-config>
